dependencies:
  - pip:
      # Core CLI and UI dependencies
      - click>=8.0.0
      - rich>=12.0.0
      - typer>=0.7.0
      - tabulate>=0.9.0
      
      # Azure dependencies
      - azure-identity>=1.12.0
      - azure-core>=1.24.0
      
      # HTTP and async dependencies
      - requests>=2.28.0
      - aiohttp>=3.8.0
      - asyncio-throttle>=1.0.0
      
      # Data processing dependencies
      - pandas>=1.5.0
      - pydantic>=1.10.0,<3.0.0
      
      # Configuration and serialization
      - PyYAML>=6.0
      - python-dotenv>=0.19.0
      
      # Security - version constrained to avoid conflicts
      - cryptography>=41.0.5,<46.0.0
      
      # Main package
      - pvw-cli==1.0.8
