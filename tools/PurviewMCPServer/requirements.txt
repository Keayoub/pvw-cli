# MCP Server Dependencies
#mcp>=1.0.0
fastmcp>=0.2.0
aiohttp>=3.8.0
azure-identity>=1.12.0
pandas>=1.5.0
pvw-cli
# Parent package - install the main pvw-cli package
# Install with: pip install -e ..
# Or from PyPI: pip install pvw-cli
